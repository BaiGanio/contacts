export const environment = {
  // Playwright e2e suite: points at the dedicated e2e API instance (see
  // tests/Contacts.E2E/playwright.config.ts), never the normal dev API.
  apiBaseUrl: 'http://localhost:5197',
};

export const environment = {
  // The API is deployed separately (Pi5 k3s cluster), not on the same origin as
  // this GitHub Pages site, so this must be an absolute URL.
  apiBaseUrl: 'https://contacts-api.baiganio.io',
};

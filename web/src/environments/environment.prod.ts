export const environment = {
  // Production: empty means "same origin" (e.g. the API is reverse-proxied
  // under /api on the host that serves this app). Set an absolute URL here
  // before building if the API is deployed on a different origin.
  apiBaseUrl: '',
};
